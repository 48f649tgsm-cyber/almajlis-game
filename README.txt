ALMAJLIS BUILD 108 ROLLBACK

Upload:
1) Replace repository index.html with this index.html.
2) Keep the existing build108.js in the repository.
3) build109.js may remain in the repository, but this index.html does NOT load it.
4) Do not delete or modify question, image, audio, or JSON files.

After upload, open the game with ?v=rollback108
